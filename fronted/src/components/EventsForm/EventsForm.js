import React from 'react'
import { Button } from 'react-bootstrap'

import './EventsForm.css'

const EventsForm = (props) => (
    <div>
        {/* <header className="modal__header">
            <h1>{props.title}</h1>
        </header>
        <section>
            {props.children}
            <div
                className="d-grid gap-2"
                style={{
                    width: '100%',
                    height: '5rem',
                    paddingLeft: '2rem',
                    paddingRight: '2rem',
                }}
            >
                <Button
                    variant="primary"
                    size="lg"
                    onClick={props.onConfirm}
                    style={{
                        fontSize: '2rem',
                        borderRadius: '10px',
                    }}
                >
                    Boka
                </Button>
            </div>
        </section>

        <div
            style={{
                textAlign: 'center',
                fontSize: '1.2rem',
                color: '#4876BA',
                fontFamily: 'Roboto Condensed',
                fontWeight: 'bold',
                padding: '2rem',
            }}
        >
            <p>
                OBS! Om du vill boka flera tjänster räcker det med att du bokar en tjänst och säger
                till önskemål när du lämnar bilen hos oss.
            </p>
        </div> */}
    </div>
)

export default EventsForm
